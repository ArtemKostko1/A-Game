<div class="gamesShop">
    <div class="row">
        {{#each shopGames}}
            <div class = "col s12 m4">
                <div class="card large game_wrapper">
                    <div class="card-image waves-effect waves-block waves-light">
                        <img class="activator" src="{{imgUrl}}" alt="{{name}}">
                    </div>
                    <div class="card-content">
                        <span class="card-title activator grey-text text-darken-4">{{name}}<i class="material-icons right">more_vert</i></span>
                        <p class="ganre">{{ganre}}</p>
                        <p class="ageLimit">{{ageLimit}}</p>
                    </div>
                    <div class="card-reveal">
                        <span class="card-title grey-text text-darken-4">{{name}}<i class="material-icons right">close</i></span>
                        <p class="developer">Developer: <span>{{developer}}</span></p>
                        <p class="releaseDate">Release Date: <span>{{releaseDate}}</span></p>
                        <p class="description">Description: <span>{{description}}</span></p>
                    </div>
                    <div class="card-action">
                        <div class="row">
                            <div class="col s12 m6">
                                <form action="/library/addToLibrary" method="POST">
                                    <input type="hidden" name="id" value="{{id}}">
                                    <button class="card-action_btn waves-effect btn-large">Add to library</button>
                                </form>
                            </div>
                            <div class="col s12 m6">
                                <form action="" method="POST">
                                    <input type="hidden" name="id" value="{{id}}">
                                    <button class="card-action_btn waves-effect btn-large">Raiting</button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
</div>